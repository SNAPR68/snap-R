diff --git a/apps/processor/src/index.ts b/apps/processor/src/index.ts
index 0000000..0000000 100644
--- a/apps/processor/src/index.ts
+++ b/apps/processor/src/index.ts
@@
       updatePhotoStatus,
       updateListingPreparationStatus,
+      incrementUsageIfNotCounted,
       getListingPhotos,
@@
-        await updateJobStatus(jobId, 'completed', env);
-        await updateListingPreparationStatus(listingId, 'prepared', env);
+        await updateListingPreparationStatus(listingId, 'prepared', env);
+        await incrementUsageIfNotCounted(listingId, userId, env);
+        await updateJobStatus(jobId, 'completed', env);
