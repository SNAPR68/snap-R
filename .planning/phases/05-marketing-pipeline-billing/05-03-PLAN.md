# Plan 05-03: Billing Gate Helper + Marketing Banner Polish

## Goal
Add the `canGenerateVideo` billing helper and polish any remaining integration points.

## Tasks

### Task 1: Billing Helper Export
`canGenerateVideo` is already added to PLAN_LIMITS in 05-01.
Add exported convenience function to `lib/content/limits.ts`:
```typescript
export function canGenerateVideo(plan: string): boolean {
  return getPlanLimits(plan).canGenerateVideo
}
```

### Task 2: Marketing Banner — Completed State Video Indicator
Update the completed state in `marketing-banner.tsx` to show a video indicator when video is complete:
```
{hasVideo && (
  <>
    <span className="text-white/20">|</span>
    <Video className="w-3 h-3 text-pink-400" />
    <span className="text-pink-300">Video ready</span>
  </>
)}
```

This is minimal — the heavy UI work is in 05-02.

## Files Changed
- `lib/content/limits.ts` (MODIFY — already partially done in 05-01)
- `components/marketing-banner.tsx` (MODIFY — polish completed state)
